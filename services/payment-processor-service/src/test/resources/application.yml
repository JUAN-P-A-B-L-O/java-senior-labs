spring:
  rabbitmq:
    listener:
      simple:
        auto-startup: false
        retry:
          enabled: true
          max-attempts: 3
          initial-interval: 2s
          multiplier: 1
          max-interval: 2s

payment-processor:
  authorization-url: https://util.devi.tools/api/v2/authorize
